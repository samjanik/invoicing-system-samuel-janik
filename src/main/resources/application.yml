spring:
  profiles:
    active: file
  flyway:
    enabled: false
  security:
    user:
      name: user
      password: password

invoicing-system:
  allowedOrigins: http://localhost:4200
  csrf:
    disable: true

server:
  port: 8080

logging:
  level:
    org:
      springframework:
        boot: INFO
    pl:
      futurecollars:
        invoicing: INFO
        invoicing.db: DEBUG




