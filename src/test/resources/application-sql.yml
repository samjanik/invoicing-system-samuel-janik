spring:
  datasource:
    url: jdbc:h2:mem:db-sql;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

invoicing-system:
  database: sql